/*
 * AuthFormConfirmed Messages
 *
 * This contains all the text for the AuthFormConfirmed container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuthFormConfirmed';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthFormConfirmed container!',
  },
});
